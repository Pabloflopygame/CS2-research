help("list")
# sacanos las fechas
dates <- list()
for (i in seq_along(data_list)) {
dates[[i]] <- data_list[[i]][1, ]
}
help(vector)
# sacanos las fechas
dates <- vector("list", length(data_list))
library(jsonlite)
setwd(".")
data_list <- fromJSON("./Data/price-history-daily.json")
# genera una lista como:
# [pistola, matrix 3 x n_observaciones, rifle, matrix 3 x n_observaciones, ...]
# Cada matrix a su vez es realmente una lista (bidimencional, pero type list) como:
# [
# fecha_observación, fecha_observación, ...
# precio,            precio,            ...
# oferta,            oferta,            ...
# ]
# *La oferta es cuanta gente esta poniendo a la venta ese item en particular
# Realmente la gente puede ponerlo a la venta en diferentes cantidades, pero
# para que los datos no pesen Tb's, lo hemos simplificado a:
#
# queremos generar un dataframe datos con columnas como:
# nombre, fecha1, fecha2, fecha3, ...
# Para cada fecha queremos que haya una lista con [oferta: precio]
# sacamos los nombres
weapons <- names(data_list)
# sacanos las fechas
dates <- vector("list", length(data_list))
for (i in seq_along(data_list)) {
dates[[i]] <- data_list[[i]][1, ]
}
dates <- unique(unlist(dates, use.names = FALSE))
dates <- as.Date(as.POSIXct(dates, origin = "1970-01-01", tz = "UTC"))
View(data_list)
dates
dates <- list()
for (i in seq_along(data_list)) {
dates[[i]] <- data_list[[i]][1, ]  # o [[1]]
}
dates <- unique(unlist(dates, use.names = FALSE))
dates <- as.Date(as.POSIXct(dates, origin = "1970-01-01", tz = "UTC"))
rm(i)
help("unlist")
dates <- list()
for (i in seq_along(data_list)) {
dates[[i]] <- data_list[[i]][1, ]  # o [[1]]
}
rm(i)
dates
View(data_list)
View(dates)
# sacamos los nombres
weapons <- names(data_list)
# sacanos las fechas
dates <- list()
for (i in seq_along(data_list)) {
dates[[i]] <- data_list[[i]][1, ]  # o [[1]]
}
rm(i)
# como tenemos para [0] (del arma índice 0) --> (fecha1, fecha2, fecha3), [1] --> (...)
# tenemos que unlist it, quitar el use.name (para que no haga nada raro tratando
# de conservar la idea que estaban en sus propios vectores cambiando los nombres)
# Y ya dsp hacemos unique para quedarnos con las fechas sin repeticiones.
dates <- unique(unlist(dates, use.names = FALSE))
dates <- as.Date(as.POSIXct(dates, origin = "1970-01-01", tz = "UTC"))
type(weapons)
typeof(weapons)
data <- data.frame(
nombre = nombres,
matrix(NA, nrow = length(nombres), ncol = length(dates))
)
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates))
)
View(data)
data
data <- data.frame(
nombre = weapons,
dates,
matrix(NA, nrow = length(weapons), ncol = length(dates))
)
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates)),
check.names = FALSE
)
df <- data.frame(matrix(ncol = length(dates), nrow = 0), check.names = FALSE)
View(df)
df <- data.frame(matrix(ncol = length(dates), nrow = 0))
View(df)
df <- data.frame(matrix(ncol = length(dates), nrow = 0), check.names = FALSE)
View(df)
View(data)
head(data)
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates)),
check.names = FALSE
)
View(data)
head(data)
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates)),
check.names = FALSE
)
colnames(data) <- c("nombre", as.character(dates))
View(data)
# Ahora que tenemos las columnas podemos construir el dataframe y llenarlo de NA's
# por ahora.
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates)),
)
# Ahora que tenemos las columnas podemos construir el dataframe y llenarlo de NA's
# por ahora.
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates))
)
# realmente no hemos añadido dates, sino que matrix le ha puesto
colnames(data) <- c("nombre", as.character(dates))
data <- data.frame(
matrix(NA, nrow = length(weapons), ncol = length(dates))
)
# realmente no hemos añadido nombres en la columna, sino que matrix le ha puesto
# valores por defecto, ahora simplemente lo sobrescribimos.
colnames(data) <- c("nombre", as.character(dates))
# Ahora que tenemos las columnas podemos construir el dataframe y llenarlo de NA's
# por ahora.
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates))
)
# realmente no hemos añadido nombres en la columna, sino que matrix le ha puesto
# valores por defecto, ahora simplemente lo sobrescribimos.
colnames(data) <- c("nombre", as.character(dates))
View(data_list)
View(data_list)
data_list[Snack Attack (Field-Tested)]
data_list["Snack Attack (Field-Tested)"]
data_list["Glock-18 | Snack Attack (Field-Tested)"]
head(data_list["Glock-18 | Snack Attack (Field-Tested)"])
head(data_list["Glock-18 | Snack Attack (Field-Tested)"])
tail(data_list["Glock-18 | Snack Attack (Field-Tested)"])
data_list["Glock-18 | Snack Attack (Field-Tested)"][0]
data_list["Glock-18 | Snack Attack (Field-Tested)"][,1]
data_list["Glock-18 | Snack Attack (Field-Tested)"][1,1]
data_list["Glock-18 | Snack Attack (Field-Tested)",1,1]
data_list["Glock-18 | Snack Attack (Field-Tested)",1]
data_list["Glock-18 | Snack Attack (Field-Tested)",]
data_list["Glock-18 | Snack Attack (Field-Tested)"]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[,1]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[0]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[3]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[6]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[0]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[0, 1]
(data_list["Glock-18 | Snack Attack (Field-Tested)"])[0]
data_list[0]
data_list[0, 1]
(data_list[0])[1, 1]
typeof(data_list["Glock-18 | Snack Attack (Field-Tested)"])
dim(data_list["Glock-18 | Snack Attack (Field-Tested)"])
size(data_list["Glock-18 | Snack Attack (Field-Tested)"])
sizeof(data_list["Glock-18 | Snack Attack (Field-Tested)"])
length(data_list["Glock-18 | Snack Attack (Field-Tested)"])
# Ahora toca lo más divertido! Ir arma por arma, añadiendo en las fechas
# que tengan registros sus ofertas y valor :3
## --- INIT LIST-COLUMNS: convierte columnas de fecha en columnas de listas ---
for (j in seq_along(dates)) {
# +1 porque la 1ª columna es "nombre"
data[[j + 1]] <- vector("list", length(weapons))
}
## --- HELPER para parsear fechas por ítem (acepta epoch o ISO) ---
.parse_dates <- function(x) {
if (is.numeric(x)) {
as.Date(as.POSIXct(x, origin = "1970-01-01", tz = "UTC"))
} else {
as.Date(x)
}
}
## --- RELLENO: para cada arma, colocar en cada fecha la lista [oferta: precio] ---
for (w in seq_along(weapons)) {
item <- data_list[[w]]
# Si tu objeto es matriz 3xn usa [fila, ]; si es lista usa [[1]], [[2]], [[3]]
dvec <- item[1, ]              # fechas (por-ítem)
pvec <- as.numeric(item[2, ])  # precios
ovec <- as.numeric(item[3, ])  # ofertas
dvec <- .parse_dates(dvec)
# Posición de cada fecha del ítem dentro del vector global 'dates'
col_idx <- match(dvec, dates)  # valores 1..length(dates)
# Construye la estructura pedida: [oferta: precio]
# -> lista nombrada cuyo nombre es la oferta y el valor es el precio
cell_list <- Map(function(off, pr) setNames(list(pr), as.character(off)), ovec, pvec)
# Asigna cada observación a su columna-fecha correspondiente (+1 por columna 'nombre')
for (k in seq_along(col_idx)) {
j <- col_idx[k]
if (!is.na(j)) {
data[[j + 1]][[w]] <- cell_list[[k]]
}
}
}
library(jsonlite)
setwd(".")
data_list <- fromJSON("./Data/price-history-daily.json")
# genera una lista como:
# [pistola, matrix 3 x n_observaciones, rifle, matrix 3 x n_observaciones, ...]
# Cada matrix a su vez es realmente una lista (bidimencional, pero type list) como:
# [
# fecha_observación, fecha_observación, ...
# precio,            precio,            ...
# oferta,            oferta,            ...
# ]
# *La oferta es cuanta gente esta poniendo a la venta ese item en particular
# Realmente la gente puede ponerlo a la venta en diferentes cantidades, pero
# para que los datos no pesen Tb's, lo hemos simplificado a:
#
# queremos generar un dataframe datos con columnas como:
# nombre, fecha1, fecha2, fecha3, ...
# Para cada fecha queremos que haya una lista con [oferta: precio]
# sacamos los nombres
weapons <- names(data_list)
# sacanos las fechas
dates <- list()
for (i in seq_along(data_list)) {
dates[[i]] <- data_list[[i]][1, ]  # o [[1]]
}
rm(i)
# como tenemos para [0] (del arma índice 0) --> (fecha1, fecha2, fecha3), [1] --> (...)
# tenemos que unlist it, quitar el use.name (para que no haga nada raro tratando
# de conservar la idea que estaban en sus propios vectores cambiando los nombres)
# Y ya dsp hacemos unique para quedarnos con las fechas sin repeticiones.
dates <- unique(unlist(dates, use.names = FALSE))
dates <- as.Date(as.POSIXct(dates, origin = "1970-01-01", tz = "UTC"))
# Ahora que tenemos las columnas podemos construir el dataframe y llenarlo de NA's
# por ahora.
data <- data.frame(
nombre = weapons,
matrix(NA, nrow = length(weapons), ncol = length(dates))
)
# realmente no hemos añadido nombres en la columna, sino que matrix le ha puesto
# valores por defecto, ahora simplemente lo sobrescribimos.
# *Añadimos nombre simplemente para aprobechar para meter los nombres con ella.
colnames(data) <- c("nombre", as.character(dates))
# Ahora toca lo más divertido! Ir arma por arma, añadiendo en las fechas
# que tengan registros sus ofertas y valor :3
library(jsonlite)
# Cargar JSON
data_list <- fromJSON("./Data/price-history-daily.json")
weapons <- names(data_list)
# Fechas únicas globales
dates <- unique(unlist(lapply(data_list, function(x) x[1, ]), use.names = FALSE))
dates <- as.Date(as.POSIXct(dates, origin="1970-01-01", tz="UTC"))
# Prealocamos matrices
nW <- length(weapons); nD <- length(dates)
precio_mat <- matrix(NA_real_, nrow = nW, ncol = nD,
dimnames = list(weapons, as.character(dates)))
oferta_mat <- matrix(NA_real_, nrow = nW, ncol = nD,
dimnames = list(weapons, as.character(dates)))
# Relleno vectorizado (rápido)
for (w in seq_along(weapons)) {
item <- data_list[[w]]
dvec <- as.Date(as.POSIXct(item[1, ], origin="1970-01-01", tz="UTC"))
pvec <- as.numeric(item[2, ])
ovec <- as.numeric(item[3, ])
idx <- match(dvec, dates)
precio_mat[w, idx] <- pvec
oferta_mat[w, idx] <- ovec
}
library(purrr)
library(tibble)
data <- tibble(
nombre = rownames(precio_mat)
)
for (j in seq_along(dates)) {
d <- as.character(dates[j])
data[[d]] <- map(seq_len(nW), function(i) {
if (is.na(precio_mat[i, j])) {
NA
} else {
list(oferta = oferta_mat[i, j], precio = precio_mat[i, j])
}
})
}
View(data)
getwd()
write_csv(data, "output.csv")
library(readr)
write_csv(data, "output.csv")
write.csv(data, file = "output.csv")
saveRDS(data, "output.rds")
